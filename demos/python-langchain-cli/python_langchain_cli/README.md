# Dewy LangChain Python CLI Demo

```
poetry run python -m python_langchain_cli add-file <path or url>
echo "what is retrieval augmented generation" | poetry run python -m python_langchain_cli query
```